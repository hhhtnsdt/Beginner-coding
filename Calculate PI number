#include <stdio.h>
#include <math.h>
int main()
{
    float i=0,n=0;
    float p=0;
    do
    {
        p += pow(-1,n)/(2*n+1);
        n+=1;
        i+=1;
    }
    while (i<=1000);
    printf("%f",p*4);
    return 0;
}
